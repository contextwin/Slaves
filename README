#!まだ正常に動作しません！

git clone https://github.com/contextwin/Slaves.git

実行要件:
xmemwrdがビルトインの機能しかインストールされていないlinuxで動作するかはまだ検証しておりません。

ビルド要件:
xmemwordはビルド要件として、xorg-devに依存します。
aptコマンドで導入する場合,
sudo apt install xorg-dev
を実行して下さい。

その他ビルド要件として、libsdl2-dev群に依存しています。
sudo apt install libsdl2-dev libsdl-image1.2-dev libsdl2-ttf-dev

RPGゲーム
設定資料
http://contextwin.livedoor.blog/archives/5307375.html
